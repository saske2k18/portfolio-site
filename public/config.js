const CONFIG = {
  // Supabase Cloud Configuration
  // If blank, the site falls back to Local Mode (local Express API + folders).
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};

export default CONFIG;
